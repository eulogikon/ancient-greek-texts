# Landing-page improvement pass — README + llms.txt (this repo)

Scope confirmed: **README.md + llms.txt/manifest surfaces in this repo**. The hosted Pages catalog (generated in the separate `ancient-greek-site` repo) is out of scope for this pass. No public JSON corpus exists or will be added (corpus-moat policy CDP-049 stands).

## Verdict on the pasted suggestions (decision record)

- **Adapt**: format matrix → reframe as "entry point by job" (real channels: md/txt/pdf, manifest CSV/JSON, HF parquet, Zenodo); code snippets → Python/pandas + `load_dataset`, not fake `requests.json`; llms-full.txt → full work-level *index*, not the corpus itself.
- **Already done**: badges (5 exist), bash quick-start, AI-training section, author navigation (hosted catalog A–Z + MANIFEST.md).
- **Reject**: JSON row/snippet (no public JSON by design); whole-corpus-in-llms-full.txt (hundreds of MB in git; bulk is Zenodo/HF/clone); author accordion in README (~100 KB bloat, duplicates catalog + MANIFEST.md, in a machine-stamped file).

## 1. Backend: `scripts/build_manifest.py` (local only — never committed)

Style per AGENTS.md: Black 88-col, isort, Ruff, pyright strict, docstrings.

**a. Word counting + stamping.** Add a word-count pass over `grc/*.grc.txt` (whitespace tokenization, Greek text only — not `en/` pages). Extend `stamp_release_metadata()` with a words phrase pattern alongside the existing `_COUNT_PHRASE`/`_COUNT_BULLET`, stamped as "≈46 million words" (or whatever the computed figure is) into: README.md headline + "What's here" + embedded JSON-LD description; llms.txt blockquote; `dataset.jsonld` description and `size` (value = precise count, unitText "words"). The precise count also goes into the generated `counts` blocks of manifest.json/MANIFEST.md. Compute per run (no cache — maintainer-triggered, seconds of runtime).

**b. `llms-full.txt` emitter.** New generated public artifact: every work (4,055) grouped by author, one line per work with `[eul_wid]` and raw.githubusercontent URLs for `.md`/`.txt`/`.pdf`. Header states counts + "generated — do not edit". Also: add it to MANIFEST.md's "Sibling indexes" list and link it from llms.txt ("Optional" section) and README.

**c. llms.txt generator fixes.** Correct the licence line ("CC0 1.0" flat → texts are Public Domain Mark 1.0, scaffolding CC0). Re-running also picks up the already-coded but never-emitted "Multilingual catalog web entry (for-agents)" line (script is newer than the committed output). Add a hosted-catalog link alongside it.

## 2. Public: README.md (hand-maintained parts)

- **Headline stat**: add the stamped word count to the intro sentence (line 24) — "1,353 authors, 4,055 works, ≈46 million words".
- **Formats section → "Formats & entry points"**: keep the compact ✓ table; add a job-oriented table routing users: read/browse → eulogikon.org + PDFs; clean text for DH/NLP → `grc/*.md|txt` resolved via `manifest.works.min.csv`; structured index → CSV/`manifest.json`; bulk training → HF parquet / Zenodo / clone; AI inference → `llms.txt` → `llms-full.txt` → raw fetch.
- **Python quick-start** after the existing bash block: `pandas.read_csv` on `manifest.works.min.csv` filtered by `eul_wid`, plus `load_dataset("eulogikon/ancient-greek-texts")` (verify actual config/split names against the HF dataset card at implementation time).
- **Consistency fixes**: correct the "~80,000-line manifest.json" claim against the real file; domain list 17 → 19 (add lexicography, scholia); mention `llms-full.txt` in "What's here" and the AI section.
- **No new badges** — 5 exist; a counts badge would be drift-prone ornament when the headline sentence carries the same info stamped.

## 3. Execution order

1. Modify `scripts/build_manifest.py` (word count, llms-full.txt, llms.txt fixes, sibling-index list).
2. Hand-edit README.md (entry-point table, Python snippet, consistency fixes) — counts/words get stamped in step 3, so leave the stampable phrases in place.
3. Run `python3 scripts/build_manifest.py` → regenerates llms.txt, llms-full.txt, manifest.*, MANIFEST.md; stamps README.md, dataset.jsonld, CITATION.cff.
4. Lint/type-check the script (black, isort, ruff, pyright).
5. Verify (see below), then commit **public outputs only**: README.md, llms.txt, llms-full.txt, MANIFEST.md, manifest.* if changed, dataset.jsonld, CITATION.cff.
6. Local-only housekeeping: refresh stale counts in AGENTS.md (1,344/4,031 → current); add `work-reports/2026-08-25-landing-readme-pass.md` and update its README pointer (never committed).

## 4. Verification

- Counts agree across README.md, llms.txt, MANIFEST.md, manifest.json `counts`, dataset.jsonld.
- `llms-full.txt` work-entry count == manifest.works.min.csv data rows (4,055); every URL path in a spot-check sample resolves against `git ls-files` (offline check, no network needed).
- Word figure reproducible: re-running the script yields an identical stamp (idempotent).
- `git status` before commit shows only the intended public files; scripts/, work-reports/, AGENTS.md excluded.

## Out of scope (recorded for later)

Hosted-catalog design changes (ancient-greek-site repo), Kaggle publication (upload script exists, dataset not yet published — not badged or linked), the 2 local-only `.epub` files (gitignored; formats stay pdf/md/txt in public claims).